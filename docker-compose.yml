version: "3.9"
services:
  openq-api:
    build:
      dockerfile: Dockerfile.dev # builds with development image for live reloads
      context: .
    ports:
      - "8090:8090"
    env_file:
      - .env.docker
    volumes:
      - /app/node_modules
      - ./:/app
    